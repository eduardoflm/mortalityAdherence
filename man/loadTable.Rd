% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{loadTable}
\alias{loadTable}
\title{Load a Built-in Mortality Table}
\usage{
loadTable(name)
}
\arguments{
\item{name}{\code{character} -- name of the table to load.
Use \code{\link{listTables}()} to see all valid options:
\code{"AT-2000bm"}, \code{"AT-2000m"}, \code{"AT-49M"},
\code{"AT-55m"}, \code{"AT-83ms"}, \code{"GAM71m"},
\code{"GAM83m"}, \code{"BR-EMS2010"}, \code{"BR-EMS2015"},
\code{"BR-EMS2021"}.}
}
\value{
A \code{data.frame} with two columns:
\describe{
  \item{\code{age}}{Integer age from 0 to 120.}
  \item{\code{qx}}{Annual probability of death at age \code{age}.}
}
}
\description{
Retrieves one of the 10 biometric mortality tables built into the
\pkg{mortalityAdherence} package as a tidy data frame.
}
\details{
The returned \code{data.frame} can be passed directly to the
\code{table} argument of \code{\link{testAdherence}()}, or used as
a starting point for constructing an adjusted table:

\preformatted{
# Apply a 10\% longevity improvement factor
tab <- loadTable("AT-2000m")
tab$qx <- tab$qx * 0.90
result <- testAdherence(fund_data, table = tab)
}

To use a completely external table (from a file or external source),
build a \code{data.frame} with columns \code{age} and \code{qx} and
pass it directly to \code{\link{testAdherence}()} -- no need to use
this function.
}
\examples{
# Load the AT-2000m table
tab <- loadTable("AT-2000m")
head(tab)

# Check qx at age 65 for all built-in tables
sapply(listTables()$name, function(nm) {
  loadTable(nm)[loadTable(nm)$age == 65, "qx"]
})

# Plot the mortality curve
tab <- loadTable("BR-EMS2021")
plot(tab$age, tab$qx, type = "l", log = "y",
     main = "BR-EMS2021 Mortality Curve",
     xlab = "Age", ylab = "q(x)")

}
\seealso{
\code{\link{listTables}}, \code{\link{testAdherence}},
\code{\link{mortality_tables}}
}
